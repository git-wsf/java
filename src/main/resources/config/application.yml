spring:
  application:
    name: yangliuxin
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/DB_YANGLIUXIN?useUnicode=true&characterEncoding=utf8&useSSL=false&zeroDateTimeBehavior=convertToNull
    username: root
    password: JUXINTaifu2015
    initialSize: 1
    minIdle: 10
    maxActive: 10
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 30000
    validationQuery: select 1
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxPoolPreparedStatementPerConnectionSize: 20
    filters: stat,log4j,wall
    connectionProperties: druid.stat.mergeSql=true;druid.st
  jpa:
    hibernate:
      ddl-auto: none
      naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      format-sql: true
    show-sql: true

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    joda-date-time-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      indent-output: true
  mvc:
    servlet:
      load-on-startup: 1
    favicon:
      enabled: false
    view:
      prefix: /
      suffix: .html
  redis:
    host: localhost
    port: 6379
    timeout: 2000
    lettuce:
      pool:
        min-idle: 0
        max-idle: 8
        max-active: 8
        max-wait: -1ms
  http:
    multipart:
      max-request-size: 20MB
      max-file-size: 20MB
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude: WEB-INF/**
files:
  path: /weblog/logs/images
token:
  expire:
    seconds: 8640000
  jwtSecret: VIVO_WEDGE2_CN_%^*@#!_2018_&*7876

validate:
  product: Dysmsapi
  domain: dysmsapi.aliyuncs.com
  accessKeyId: LTAIzmtd6XjOmlCs
  accessKeySecret:  Urz2QYFxJI69jh9hK578mciWMO5jaL
  iosAppVersion: 1.0.0
  androidAppVersion: 1.1.0
  code:
    count: 8
    expireIn: 1800

server:
  port: 8000
  tomcat:
      uri-encoding: UTF-8
      basedir: .
      accesslog:
        enabled: true
        directory: /weblog/logs/
        pattern: combined
        prefix: tomcat_access
  servlet-path: /
  #context-path: /boot

#debug: true
logging:
  level: debug
  file: /weblog/logs/yangliuxin.log

flyway:
  enabled: true
  baseline-on-migrate: true
  encoding: UTF-8
  locations: classpath:db/migration

weChat:
  appId: wx3d5abb2cb396a10f
  secret: c97349cba93ee239af1b3e586b493111
  token: weChatToken
  expire: 72000
  siteUrl: https://vivo.wedge2.cn/
  gift:
    giftList:
    - {id: 1,name: 50元话费券, probability: 0.001, perDay: 1, total: 50}
    - {id: 2,name: 爱奇艺VIP充值卡, probability: 0.01, perDay: 2, total: 100}
    - {id: 3,name: 5元话费券, probability: 0.05, perDay: 6, total: 300}
    - {id: 4,name: V钻, probability: 0.1, perDay: 32, total: 1630}